{
  "name": "hexo-fetch-nem",
  "version": "0.0.1",
  "description": "a hexo plugin which allow user to fetch the contents deployed on nem blockchain",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gyorou@tjjtds.me",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
    "eslint": "^4.18.1"
  }
}
